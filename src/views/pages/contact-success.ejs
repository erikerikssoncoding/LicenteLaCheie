<%- include('../partials/head', { title, description, request }) %>
<%- include('../partials/header') %>
<section class="py-5 py-lg-6 text-center">
  <div class="container">
    <div class="glass-card mx-auto" style="max-width: 560px;">
      <h1 class="section-title mb-3">Multumim!</h1>
      <p class="section-subtitle mb-0">
        Solicitarea ta a fost inregistrata. Un consultant Licente la Cheie te va contacta in scurt timp pentru urmatorii pasi.
      </p>
      <% if (currentUser) { %>
      <p class="mt-3">Ticketul dedicat a fost adaugat in sectiunea „Tichetele mele”.</p>
      <% } %>
      <a class="btn btn-gradient mt-4" href="/">Intoarce-te la pagina principala</a>
    </div>
  </div>
</section>
<%- include('../partials/footer') %>
