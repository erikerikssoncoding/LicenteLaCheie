<%- include('../partials/head', { title, description, request }) %>
<%- include('../partials/header') %>
<section class="py-5">
  <div class="container">
    <div class="card shadow-sm">
      <div class="card-body">
        <h1 class="h3 mb-3">Solicitarea de oferta a fost inregistrata</h1>
        <p>
          Codul tau de referinta este <strong><%= offerCode %></strong>. Il poti folosi pentru a urmari contractul si discutiile
          in contul tau.
        </p>
        <p>
          In maxim <strong><%= defaultExpiration %> ore</strong> un administrator iti va trimite oferta cu pretul final si termenul
          de valabilitate direct in ticketul creat automat.
        </p>
        <div class="alert alert-info mt-4">
          <p class="mb-1">Ticketul tau dedicat poate fi accesat dupa autentificare din panoul „Tichetele mele”.</p>
          <p class="mb-0">ID ticket: <strong>#<%= ticketId %></strong></p>
        </div>
        <% if (generatedPassword) { %>
        <div class="alert alert-success mt-3">
          <p class="mb-1">Am creat un cont pentru tine pe licentelacheie.ro.</p>
          <p class="mb-0">Email: <strong><%= submissionEmail %></strong> | Parola temporara: <strong><%= generatedPassword %></strong></p>
          <small class="text-muted">Te rugam sa o schimbi din sectiunea „Cont” dupa prima autentificare.</small>
        </div>
        <% } else { %>
        <p class="mt-3">Te poti autentifica folosind datele existente pentru a vizualiza si accepta oferta.</p>
        <% } %>
        <a class="btn btn-primary mt-4" href="/autentificare">Autentifica-te in platforma</a>
      </div>
    </div>
  </div>
</section>
<%- include('../partials/footer') %>
