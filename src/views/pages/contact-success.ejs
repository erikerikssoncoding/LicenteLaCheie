<%- include('../partials/head', { title, description, request }) %>
<%- include('../partials/header') %>
<section class="py-5 py-lg-6 text-center">
  <div class="container">
    <div class="glass-card mx-auto" style="max-width: 560px;">
      <h1 class="section-title mb-3">Mulțumim!</h1>
      <p class="section-subtitle mb-0">
        Solicitarea ta a fost înregistrată. Un consultant Licențe la Cheie te va contacta în scurt timp pentru următorii pași.
      </p>
      <% if (currentUser) { %>
      <p class="mt-3">Ticketul dedicat a fost adăugat în secțiunea „Tichetele mele”.</p>
      <% } %>
      <a class="btn btn-gradient mt-4" href="/">Întoarce-te la pagina principală</a>
    </div>
  </div>
</section>
<%- include('../partials/footer') %>
