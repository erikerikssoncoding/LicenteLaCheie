<%- include('../partials/head', { title, description, request }) %>
<%- include('../partials/header') %>
<section class="py-5 py-lg-6 text-center">
  <div class="container">
    <div class="glass-card mx-auto" style="max-width: 560px;">
      <h1 class="section-title mb-3">Mulțumim!</h1>
      <p class="section-subtitle mb-0">
        Solicitarea ta a fost înregistrată. Un consultant de la Academia de Licențe te va contacta în scurt timp pentru următorii pași.
      </p>
      <% if (currentUser) { %>
      <p class="mt-3">Ticketul dedicat a fost adăugat în secțiunea „Tichetele mele”.</p>
      <% } %>
      <% if (ticketDisplayCode || ticketId) { %>
      <p class="mt-3">
        Cod ticket: <strong><%= ticketDisplayCode || `#${ticketId}` %></strong> – îl poți urmări din contul tău din platforma Academia de Licențe.
      </p>
      <% } %>
      <% if (generatedPassword) { %>
      <div class="alert alert-success mt-4">
        <p class="mb-1">Ți-am creat automat un cont pentru a urmări discuția.</p>
        <p class="mb-0">Email: <strong><%= submissionEmail %></strong> | Parolă temporară: <strong><%= generatedPassword %></strong></p>
        <small class="text-muted">Autentifică-te și schimbă parola din secțiunea „Cont”.</small>
      </div>
      <% } else if (!currentUser && submissionEmail) { %>
      <p class="mt-3">Ți-am trimis confirmarea la <strong><%= submissionEmail %></strong>.</p>
      <% } %>
      <a class="btn btn-gradient mt-4" href="/">Întoarce-te la pagina principală</a>
    </div>
  </div>
</section>
<%- include('../partials/footer') %>
