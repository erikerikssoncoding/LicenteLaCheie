<%- include('../partials/head', { title, description, request }) %>
<%- include('../partials/header') %>
<section class="py-5">
  <div class="container">
    <div class="card shadow-sm">
      <div class="card-body">
        <h1 class="h3">Oferta ta personalizata este gata</h1>
        <p>Codul tau de oferta este <strong><%= offerCode %></strong>. Salveaza-l pentru a accesa contractul ulterior.</p>
        <h2 class="h5 mt-4">Contract draft</h2>
        <pre class="bg-light p-3 rounded"><%= contractText %></pre>
        <a class="btn btn-primary" href="/contract/<%= offerCode %>">Vezi contractul online</a>
      </div>
    </div>
  </div>
</section>
<%- include('../partials/footer') %>
