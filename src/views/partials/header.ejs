<nav class="navbar navbar-expand-lg navbar-light premium-navbar">
  <% const isAuthenticated = Boolean(currentUser); %>
  <% const isStaff = isAuthenticated && ['redactor', 'admin', 'superadmin'].includes(currentUser.role); %>
  <% const isManager = isAuthenticated && ['admin', 'superadmin'].includes(currentUser.role); %>
  <% const unreadTicketTotal = ticketUnreadSummary?.totalUnreadCount || 0; %>
  <div class="container">
    <a
      class="navbar-brand fw-semibold"
      href="/"
      aria-label="Academia de Licențe"
    >
      <span class="brand-logo-wrapper">
        <img
          src="/img/academiadelicente-logo.webp"
          alt="Sigla Academia de Licențe"
          class="brand-logo"
          width="246"
          height="60"
        />
      </span>
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Meniu">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto align-items-lg-center gap-lg-3">
        <% if (!isStaff) { %>
        <li class="nav-item"><a class="nav-link" href="/">Acasă</a></li>
        <li class="nav-item"><a class="nav-link" href="/servicii">Servicii</a></li>
        <li class="nav-item"><a class="nav-link" href="/despre-noi">Despre noi</a></li>
        <li class="nav-item"><a class="nav-link" href="/oferta">Solicită ofertă</a></li>
        <li class="nav-item"><a class="nav-link" href="/contact">Contact</a></li>
        <% } else { %>
        <li class="nav-item"><a class="nav-link" href="/cont">Panou</a></li>
        <li class="nav-item"><a class="nav-link" href="/cont/proiecte">Proiecte</a></li>
        <li class="nav-item position-relative">
          <a class="nav-link d-inline-flex align-items-center gap-1" href="/cont/tichete">
            Tichete
            <% if (unreadTicketTotal > 0) { %>
            <span class="ticket-unread-indicator" aria-label="Ai mesaje necitite în tichete"></span>
            <% } %>
          </a>
        </li>
        <% if (isManager) { %>
        <li class="nav-item"><a class="nav-link" href="/cont/utilizatori">Utilizatori</a></li>
        <% } %>
        <% if (currentUser.role === 'superadmin') { %>
        <li class="nav-item"><a class="nav-link" href="/cont#securitate">Securitate</a></li>
        <% } %>
        <% } %>
        <% if (!currentUser) { %>
        <li class="nav-item"><a class="nav-link" href="/autentificare">Autentificare</a></li>
        <li class="nav-item"><a class="btn btn-gradient" href="/inregistrare">Creează cont</a></li>
        <li class="nav-item d-flex align-items-center">
          <a class="btn btn-outline-primary phone-call-link" href="tel:+40756856395">
            <span aria-hidden="true">📞</span>
            <span class="ms-2">0756 856 395</span>
          </a>
        </li>
        <% } else { %>
        <li class="nav-item"><a class="nav-link" href="/cont/setari">Cont</a></li>
        <li class="nav-item">
          <form action="/deconectare" method="post" class="d-inline">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
            <button class="btn btn-link nav-link" type="submit">Deconectare</button>
          </form>
        </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
<% if (currentUser && currentUser.role === 'admin' && licenseState?.isExpired) { %>
<div class="bg-warning-subtle border-bottom border-warning-subtle">
  <div class="container py-2 small text-warning-emphasis d-flex flex-column flex-lg-row align-items-lg-center gap-2">
    <span class="fw-semibold">Licența platformei a expirat.</span>
    <span class="text-muted">Unele funcții au fost dezactivate până la reînnoire. Contactează un superadmin pentru reactivare.</span>
  </div>
</div>
<% } %>
